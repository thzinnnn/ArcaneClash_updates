(() => {
  var M = {
      solar: { name: "Solar", a: "#ffba56", b: "#d95d43" },
      void: { name: "Vazio", a: "#a777ff", b: "#5441aa" },
      wild: { name: "Selvagem", a: "#70df7b", b: "#267b5d" },
      frost: { name: "Geada", a: "#70e3ff", b: "#536dc7" },
      mech: { name: "Mecânico", a: "#8edbd6", b: "#586f85" },
      arcane: { name: "Arcano", a: "#d77fff", b: "#7148b1" },
      neutral: { name: "Neutro", a: "#d6ca9e", b: "#696454" },
    },
    N = [
      {
        id: "solara",
        name: "Solara",
        fac: "solar",
        icon: "☀",
        hp: 31,
        text: "A primeira criatura que você invoca no turno recebe +1 ATQ.",
      },
      {
        id: "umbra",
        name: "Umbra",
        fac: "void",
        icon: "☾",
        hp: 29,
        text: "Seu primeiro feitiço do turno custa 1 a menos.",
      },
      {
        id: "thorn",
        name: "Thorn",
        fac: "wild",
        icon: "❀",
        hp: 32,
        text: "As duas primeiras invocações do turno curam 1 de vida.",
      },
      {
        id: "aeris",
        name: "Aeris",
        fac: "frost",
        icon: "❄",
        hp: 30,
        text: "A primeira criatura rival da rodada entra Congelada.",
      },
      {
        id: "gear",
        name: "Gear-9",
        fac: "mech",
        icon: "⚙",
        hp: 33,
        text: "A cada terceira carta jogada no turno, compre 1 da Reserva.",
      },
      {
        id: "lyra",
        name: "Lyra",
        fac: "arcane",
        icon: "✦",
        hp: 30,
        text: "Seu primeiro feitiço do turno recebe +1 de potência.",
      },
    ],
    c = (e, t, a, n, r, o, i, s = [], l = {}) => ({
      name: e,
      fac: t,
      cost: a,
      atk: n,
      hp: r,
      maxHp: r,
      icon: o,
      text: i,
      kw: s,
      type: "unit",
      ...l,
    }),
    u = (e, t, a, n, r, o, i = "none") => ({
      name: e,
      fac: t,
      cost: a,
      icon: n,
      text: r,
      effect: o,
      target: i,
      type: "spell",
    }),
    G = (e, t, a, n, r, o) => ({
      name: e,
      fac: t,
      cost: a,
      icon: n,
      text: r,
      effect: o,
      type: "relic",
    }),
    L = [
      c("Escudeiro do Alvorecer", "solar", 1, 2, 2, "🗡", "Soldado eficiente."),
      c("Guardião Solar", "solar", 2, 2, 4, "🛡", "Guarda.", ["Guarda"]),
      c("Falcão de Brasa", "solar", 2, 3, 1, "🦅", "Investida.", ["Investida"]),
      c(
        "Cavaleira Radiante",
        "solar",
        3,
        4,
        3,
        "🏇",
        "Ao entrar, fortalece um aliado.",
        [],
        { onPlay: "buffAlly" },
      ),
      c(
        "Leão do Meio-Dia",
        "solar",
        4,
        5,
        5,
        "🦁",
        "Causa +1 ao herói em rota vazia.",
        [],
        { heroBonus: 1 },
      ),
      c("Serafim Dourado", "solar", 5, 5, 6, "👼", "Escudo.", ["Escudo"]),
      u(
        "Raio Solar",
        "solar",
        2,
        "☀",
        "Cause 3 a uma criatura.",
        "damage3",
        "enemy",
      ),
      u(
        "Bênção da Aurora",
        "solar",
        2,
        "✨",
        "Dê +2/+2 a um aliado.",
        "buff22",
        "ally",
      ),
      u("Clarão", "solar", 3, "🌤", "Cause 2 a todos os inimigos.", "aoe2"),
      u("Segundo Amanhecer", "solar", 4, "🌅", "Cure 6 do seu herói.", "heal6"),
      G(
        "Estandarte Dourado",
        "solar",
        3,
        "🚩",
        "Invocações recebem +1 de vida.",
        "summonHp",
      ),
      c(
        "Rastejante do Vazio",
        "void",
        1,
        1,
        3,
        "👁",
        "Ao morrer, compre 1.",
        [],
        { death: "draw" },
      ),
      c("Assassino Sombrio", "void", 2, 3, 2, "🗡", "Investida.", [
        "Investida",
      ]),
      c("Devorador", "void", 3, 3, 5, "🕳", "Ao matar, recebe +1/+1.", [], {
        devour: !0,
      }),
      c(
        "Eco Sem Rosto",
        "void",
        3,
        4,
        3,
        "🎭",
        "Ao entrar, cause 1 ao herói.",
        [],
        { onPlay: "ping" },
      ),
      c("Arauto do Eclipse", "void", 4, 4, 5, "🌘", "Vampirismo.", [
        "Vampirismo",
      ]),
      c(
        "Horror Abissal",
        "void",
        6,
        7,
        6,
        "🐙",
        "Ao entrar, fere o inimigo mais fraco.",
        [],
        { onPlay: "weak2" },
      ),
      u(
        "Fenda",
        "void",
        2,
        "🕳",
        "Cause 2; se destruir, compre 1.",
        "rift",
        "enemy",
      ),
      u(
        "Drenar Essência",
        "void",
        3,
        "🩸",
        "Cause 3 ao herói e cure 3.",
        "drain",
      ),
      u(
        "Apagar",
        "void",
        4,
        "⬛",
        "Destrua criatura inimiga com ATQ ≤ 4.",
        "erase",
        "enemy",
      ),
      u(
        "Eco Profundo",
        "void",
        2,
        "🔮",
        "Compre 2 e perca 2 de vida.",
        "draw2hurt",
      ),
      G(
        "Olho do Abismo",
        "void",
        3,
        "👁",
        "30% de chance de compra extra no turno.",
        "voidEye",
      ),
      c("Broto Valente", "wild", 1, 1, 4, "🌱", "Guarda.", ["Guarda"]),
      c(
        "Lobo Musgoso",
        "wild",
        2,
        3,
        2,
        "🐺",
        "Recebe +1 ATQ se tiver aliado na rota.",
        [],
        { pack: !0 },
      ),
      c(
        "Xamã de Casca",
        "wild",
        3,
        2,
        5,
        "🪵",
        "Ao entrar, cure 2 dos aliados.",
        [],
        { onPlay: "healTeam" },
      ),
      c("Javali Antigo", "wild", 3, 4, 4, "🐗", "Guarda.", ["Guarda"]),
      c(
        "Druida das Esporas",
        "wild",
        4,
        3,
        6,
        "🍄",
        "No início do turno, cura um aliado.",
        [],
        { turnHeal: !0 },
      ),
      c("Urso Esmeralda", "wild", 5, 6, 7, "🐻", "Grande e resiliente."),
      u(
        "Crescimento Súbito",
        "wild",
        1,
        "🌿",
        "Dê +1/+3 a um aliado.",
        "buff13",
        "ally",
      ),
      u("Raízes", "wild", 2, "🪴", "Congele um inimigo.", "freeze", "enemy"),
      u("Renascimento", "wild", 4, "🌺", "Invoque dois Brotos.", "sprouts"),
      u(
        "Seiva Vital",
        "wild",
        3,
        "💚",
        "Cure o herói em 4 e aliados em 2.",
        "wildHeal",
      ),
      G(
        "Coração da Floresta",
        "wild",
        3,
        "🌲",
        "Com 3+ criaturas, cure 1 no fim do turno.",
        "forestHeart",
      ),
      c(
        "Pinguim Duelista",
        "frost",
        1,
        2,
        2,
        "🐧",
        "Pode congelar o defensor.",
        [],
        { onPlay: "laneFreeze" },
      ),
      c("Sentinela de Gelo", "frost", 2, 1, 5, "🧊", "Guarda.", ["Guarda"]),
      c(
        "Lâmina Boreal",
        "frost",
        3,
        4,
        3,
        "⚔",
        "25% de chance de congelar ao ferir.",
        [],
        { freezeHit: 0.25 },
      ),
      c(
        "Maga da Nevasca",
        "frost",
        4,
        3,
        5,
        "🧙",
        "Congela inimigo aleatório.",
        [],
        { onPlay: "freezeRandom" },
      ),
      c(
        "Yeti da Geleira",
        "frost",
        5,
        6,
        6,
        "❄",
        "Recebe +2 ATQ contra congelados.",
        [],
        { frostPredator: 2 },
      ),
      c(
        "Dragão Boreal",
        "frost",
        7,
        7,
        8,
        "🐉",
        "Ao entrar, congela todos os inimigos.",
        [],
        { onPlay: "freezeAll" },
      ),
      u(
        "Estilhaço",
        "frost",
        1,
        "🔹",
        "Cause 2; 25% de congelar.",
        "ice2",
        "enemy",
      ),
      u(
        "Nevasca",
        "frost",
        4,
        "🌨",
        "Cause 1 e congele todos os inimigos.",
        "blizzard",
      ),
      u(
        "Espelho de Gelo",
        "frost",
        2,
        "🪞",
        "Dê Escudo a um aliado.",
        "shield",
        "ally",
      ),
      u(
        "Zero Absoluto",
        "frost",
        5,
        "🥶",
        "Congele todas as criaturas e compre 1.",
        "zero",
      ),
      G(
        "Cristal Eterno",
        "frost",
        3,
        "💎",
        "Ao descongelar, sua criatura recebe +2 de vida.",
        "frostRelic",
      ),
      c(
        "Drone Sucateiro",
        "mech",
        1,
        2,
        1,
        "🤖",
        "Ao morrer, +1 mana no próximo turno.",
        [],
        { death: "battery" },
      ),
      c("Autômato de Bronze", "mech", 2, 2, 4, "⚙", "Confiável."),
      c(
        "Torreta Portátil",
        "mech",
        3,
        3,
        5,
        "🔫",
        "Fim do turno: cause 1 ao herói.",
        [],
        { endPing: 1 },
      ),
      c("Cão Mecânico", "mech", 3, 4, 3, "🐕", "Investida.", ["Investida"]),
      c("Golem de Cobre", "mech", 4, 5, 6, "🦾", "Guarda.", ["Guarda"]),
      c(
        "Duplicador MK-II",
        "mech",
        5,
        4,
        5,
        "🧬",
        "Ao entrar, copia parte de um aliado.",
        [],
        { onPlay: "copy" },
      ),
      u(
        "Recalibrar",
        "mech",
        1,
        "🔧",
        "Dê +1/+1 e Escudo.",
        "mechBuff",
        "ally",
      ),
      u(
        "Sobrecarga",
        "mech",
        2,
        "⚡",
        "Dê +3 ATQ neste turno; sofre 1.",
        "overclock",
        "ally",
      ),
      u(
        "Míssil Guiado",
        "mech",
        3,
        "🚀",
        "Cause 4 ao inimigo de maior ATQ.",
        "missile",
      ),
      u(
        "Reciclagem",
        "mech",
        2,
        "♻",
        "Compre 1; +1 se aliado morreu.",
        "recycle",
      ),
      G(
        "Fábrica Autônoma",
        "mech",
        4,
        "🏭",
        "A cada 3 turnos, invoque um Drone.",
        "factory",
      ),
      c(
        "Aprendiz da Faísca",
        "arcane",
        1,
        1,
        3,
        "🪄",
        "Feitiços dão +1 ATQ neste turno.",
        [],
        { spellGrow: !0 },
      ),
      c(
        "Familiar Rúnico",
        "arcane",
        2,
        2,
        3,
        "🐈",
        "Com mão pequena, compre 1.",
        [],
        { onPlay: "smallDraw" },
      ),
      c("Ilusionista", "arcane", 3, 3, 4, "🎩", "Escudo.", ["Escudo"]),
      c(
        "Elemental de Mana",
        "arcane",
        4,
        4,
        5,
        "🔷",
        "Segundo feitiço: +1/+1.",
        [],
        { manaGrow: !0 },
      ),
      c(
        "Arquimaga Lyss",
        "arcane",
        5,
        4,
        7,
        "🧙‍♀️",
        "Próximo feitiço custa 2 a menos.",
        [],
        { onPlay: "discount" },
      ),
      c(
        "Quimera Astral",
        "arcane",
        7,
        7,
        7,
        "🦄",
        "Recebe uma bênção aleatória.",
        [],
        { onPlay: "bless" },
      ),
      u(
        "Míssil Arcano",
        "arcane",
        1,
        "✨",
        "Cause 2 a qualquer criatura.",
        "arcane2",
        "any",
      ),
      u(
        "Transmutar",
        "arcane",
        3,
        "🔁",
        "Troque ATQ e vida atual.",
        "swap",
        "any",
      ),
      u(
        "Duplicar",
        "arcane",
        4,
        "🪞",
        "Crie uma cópia 1/1.",
        "duplicate",
        "ally",
      ),
      u(
        "Previsão",
        "arcane",
        2,
        "🔮",
        "Compre 2; devolva 1 aleatória.",
        "forecast",
      ),
      G(
        "Biblioteca Infinita",
        "arcane",
        4,
        "📚",
        "Mão maior e compra extra a cada 4 turnos.",
        "library",
      ),
      c("Mercenário Errante", "neutral", 2, 3, 3, "🥷", "Sem facção."),
      c("Tartaruga de Guerra", "neutral", 3, 2, 6, "🐢", "Guarda.", ["Guarda"]),
      c("Grifo Jovem", "neutral", 4, 5, 4, "🦅", "Investida.", ["Investida"]),
      c("Gigante das Pedras", "neutral", 6, 7, 8, "🗿", "Guarda.", ["Guarda"]),
      u(
        "Moeda da Sorte",
        "neutral",
        0,
        "🪙",
        "50%: +2 mana; senão compre 1.",
        "coin",
      ),
      u(
        "Mapa do Caos",
        "neutral",
        2,
        "🗺",
        "Cada jogador compra 2.",
        "bothDraw",
      ),
      u(
        "Purificação",
        "neutral",
        2,
        "💧",
        "Cure 3 e descongele aliado.",
        "purify",
        "ally",
      ),
      G(
        "Ampulheta Partida",
        "neutral",
        2,
        "⏳",
        "Pode aumentar a mana máxima.",
        "hourglass",
      ),
    ],
    me = [
      {
        name: "☄ Chuva de Meteoros",
        text: "Todas as criaturas sofrem 1.",
        effect: "meteor",
      },
      {
        name: "💎 Mana Selvagem",
        text: "Ambos ganham +1 mana máxima.",
        effect: "mana",
      },
      {
        name: "🌕 Lua de Sangue",
        text: "Heróis sofrem 2 e criaturas ganham +1 ATQ.",
        effect: "blood",
      },
      {
        name: "🌫 Neblina Mística",
        text: "Uma criatura aleatória de cada lado recebe Escudo.",
        effect: "fog",
      },
      {
        name: "🌱 Primavera Selvagem",
        text: "Todas as criaturas recuperam 2.",
        effect: "spring",
      },
      {
        name: "📜 Surto de Conhecimento",
        text: "Ambos compram 1 da Reserva.",
        effect: "knowledge",
      },
    ],
    ee = {
      common: { name: "BAÚ COMUM", icon: "🧰", color: "#d7b57d" },
      rare: { name: "BAÚ RARO", icon: "💎", color: "#6edfff" },
      legendary: { name: "BAÚ LENDÁRIO", icon: "👑", color: "#ffd45f" },
    };
  var te = "0.9.1";
  var Ne = (e) => JSON.parse(JSON.stringify(e)),
    b = (e) => e[(Math.random() * e.length) | 0],
    ye = (e) => {
      for (let t = e.length - 1; t > 0; t--) {
        let a = (Math.random() * (t + 1)) | 0;
        [e[t], e[a]] = [e[a], e[t]];
      }
      return e;
    },
    Le = () =>
      Math.random().toString(36).slice(2, 10) +
      Date.now().toString(36).slice(-5),
    F = {
      duo: {
        id: "duo",
        name: "Confronto 2v2",
        minPlayers: 4,
        maxPlayers: 4,
        teamHp: 56,
        laneCap: 4,
        startMana: 1,
        maxMana: 10,
        rerolls: 2,
      },
      raidcoop: {
        id: "raidcoop",
        name: "Raid Cooperativa",
        minPlayers: 2,
        maxPlayers: 4,
        teamHp: 48,
        laneCap: 4,
        startMana: 2,
        maxMana: 12,
        rerolls: 3,
        boss: !0,
      },
    };
  function A(e, t = null) {
    let a = Ne(e);
    return (
      (a.id = Le()),
      (a.ownerSlot = t),
      (a.age = (a.kw || []).includes("Investida") ? 1 : 0),
      (a.frozen = 0),
      (a.shield = (a.kw || []).includes("Escudo")),
      a
    );
  }
  function xe(e) {
    let t = L.filter((r) => r.fac === e.fac),
      a = L.filter((r) => r.fac === "neutral"),
      n = L.filter((r) => r.fac !== e.fac && r.fac !== "neutral");
    return { own: t, neutral: a, other: n };
  }
  function Ue(e) {
    let { own: t, neutral: a } = xe(e),
      n = [];
    for (let r = 0; r < 22; r++) n.push(A(b(t)));
    for (let r = 0; r < 8; r++) n.push(A(b(a)));
    return ye(n);
  }
  function je(e) {
    if (globalThis.ArcanaEvolution?.openingHand?.(e)) return;
    let t = L.filter((o) => o.fac === e.hero.fac),
      a = t.filter((o) => o.type === "unit" && o.cost <= 2),
      n = t.filter((o) => o.type === "unit" && o.cost >= 2 && o.cost <= 4),
      r = t.filter((o) => o.type === "spell" && o.cost <= 3);
    e.hand = [A(b(a), e.slot), A(b(n), e.slot), A(b(r), e.slot)];
  }
  function O(e, t) {
    return e.teams[t.team];
  }
  function ne(e, t) {
    return e.teams[t.team === 0 ? 1 : 0];
  }
  function Ve(e) {
    return e.team === 0 ? 1 : 0;
  }
  function y(e) {
    return e.lanes.flat();
  }
  function be(e, t) {
    return e.players.find((a) => a.slot === t.ownerSlot) || null;
  }
  function ve(e, t) {
    return e.teams[t?.team]?.lanes?.[t?.lane]?.[t?.index] || null;
  }
  function V(e) {
    return F[e.mode].laneCap;
  }
  function T(e, t) {
    e.hp = Math.min(e.maxHp, e.hp + t);
  }
  function ge(e) {
    return (
      e.mode === "duo"
        ? (e.teams[0].hp <= 0 || e.teams[1].hp <= 0) &&
          ((e.over = !0), (e.winnerTeam = e.teams[0].hp > 0 ? 0 : 1))
        : e.teams[1].hp <= 0
          ? ((e.over = !0), (e.winnerTeam = 0))
          : e.teams[0].hp <= 0 && ((e.over = !0), (e.winnerTeam = 1)),
      e.over
    );
  }
  function $(e, t, a) {
    ((t.hp -= Math.max(0, a)), Ae(e), ge(e));
  }
  function we(e) {
    return e.relics.some((t) => t.effect === "library") ? 14 : 12;
  }
  function C(e, t, a = 1) {
    for (let n = 0; n < a; n++)
      if (!t.deck.length) (t.fatigue++, $(e, O(e, t), t.fatigue));
      else if (t.hand.length < we(t)) {
        let r = t.deck.pop();
        ((r.ownerSlot = t.slot), t.hand.push(r));
      }
  }
  function ie(e, t) {
    let a = t.cost;
    return (
      t.type === "spell" && e.hero.id === "umbra" && e.spellsTurn === 0 && a--,
      t.type === "spell" && (a -= e.nextDiscount || 0),
      Math.max(0, a)
    );
  }
  function Ce(e, t, a = null) {
    for (let r of e.teams)
      for (let o of r.lanes) {
        let i = o.findIndex((s) => s.id === t.id);
        if (i >= 0) {
          o.splice(i, 1);
          break;
        }
      }
    let n = be(e, t);
    (n &&
      (n.deathsTurn++,
      t.death === "draw" && C(e, n, 1),
      t.death === "battery" && n.bonusNext++),
      a?.devour && (a.atk++, a.hp++, a.maxHp++));
  }
  function w(e, t, a, n = null) {
    return t
      ? t.shield
        ? ((t.shield = !1), 0)
        : ((t.hp -= a),
          n?.freezeHit &&
            Math.random() < n.freezeHit &&
            t.hp > 0 &&
            (t.frozen = 1),
          t.hp <= 0 && Ce(e, t, n),
          a)
      : 0;
  }
  function se(e) {
    let t = y(e);
    return t.length ? b(t) : null;
  }
  function Fe(e) {
    let t = y(e);
    return t.length ? [...t].sort((a, n) => a.hp - n.hp)[0] : null;
  }
  function qe(e) {
    let t = y(e);
    return t.length ? [...t].sort((a, n) => n.atk - a.atk)[0] : null;
  }
  function Je(e, t, a) {
    return e.players.some((n) => n.team === t && n.hero.id === a);
  }
  function le(e, t, a, n) {
    let r = O(e, t);
    if (n < 0 || n > 2 || r.lanes[n].length >= V(e)) return !1;
    let o = A(a, t.slot);
    (t.relics.some((s) => s.effect === "summonHp") && (o.hp++, o.maxHp++),
      t.hero.id === "solara" && t.unitsTurn === 0 && o.atk++);
    let i = Ve(t);
    return (
      Je(e, i, "aeris") &&
        e.teams[i].frostRound !== e.round &&
        ((o.frozen = 1), (e.teams[i].frostRound = e.round)),
      r.lanes[n].push(o),
      t.unitsTurn++,
      t.hero.id === "thorn" &&
        t.wildTriggers < 2 &&
        (t.wildTriggers++, T(r, 1)),
      _e(e, t, o, n),
      !0
    );
  }
  function _e(e, t, a, n) {
    let r = O(e, t),
      o = ne(e, t);
    if (a.onPlay === "buffAlly") {
      let i = y(r).filter((s) => s.id !== a.id);
      i.length && b(i).atk++;
    }
    if ((a.onPlay === "ping" && $(e, o, 1), a.onPlay === "weak2")) {
      let i = Fe(o);
      i && w(e, i, 2, a);
    }
    if (
      (a.onPlay === "healTeam" &&
        y(r).forEach((i) => (i.hp = Math.min(i.maxHp, i.hp + 2))),
      a.onPlay === "laneFreeze")
    ) {
      let i = o.lanes[n][0];
      i && Math.random() < 0.25 && (i.frozen = 1);
    }
    if (a.onPlay === "freezeRandom") {
      let i = se(o);
      i && (i.frozen = 1);
    }
    if (
      (a.onPlay === "freezeAll" && y(o).forEach((i) => (i.frozen = 1)),
      a.onPlay === "copy")
    ) {
      let i = y(r).filter((s) => s.id !== a.id);
      if (i.length) {
        let s = b(i);
        ((a.atk += Math.max(1, Math.floor(s.atk / 2))),
          (a.hp += Math.max(1, Math.floor(s.maxHp / 2))),
          (a.maxHp = a.hp));
      }
    }
    if (
      (a.onPlay === "smallDraw" && t.hand.length <= 3 && C(e, t, 1),
      a.onPlay === "discount" && (t.nextDiscount += 2),
      a.onPlay === "bless")
    ) {
      let i = (Math.random() * 4) | 0;
      i === 0
        ? ((a.atk += 2), (a.hp += 2), (a.maxHp += 2))
        : i === 1
          ? (a.shield = !0)
          : i === 2
            ? C(e, t, 1)
            : T(r, 3);
    }
  }
  function Qe(e, t, a, n) {
    return a.target === "none"
      ? !0
      : !(
          !ve(e, n) ||
          (a.target === "ally" && n.team !== t.team) ||
          (a.target === "enemy" && n.team === t.team)
        );
  }
  function Ye(e, t, a, n) {
    let r = O(e, t),
      o = ne(e, t),
      i = ve(e, n),
      s = t.hero.id === "lyra" && t.spellsTurn === 0 ? 1 : 0;
    if (
      (a.effect === "damage3" && w(e, i, 3 + s, a),
      a.effect === "buff22" && ((i.atk += 2), (i.hp += 2), (i.maxHp += 2)),
      a.effect === "aoe2" &&
        y(o)
          .slice()
          .forEach((l) => w(e, l, 2 + s, a)),
      a.effect === "heal6" && T(r, 6 + s),
      a.effect === "rift")
    ) {
      let l = i.hp;
      (w(e, i, 2 + s, a), l > 0 && i.hp <= 0 && C(e, t, 1));
    }
    if (
      (a.effect === "drain" && ($(e, o, 3 + s), T(r, 3 + s)),
      a.effect === "erase" && i.atk <= 4 && Ce(e, i, a),
      a.effect === "draw2hurt" && (C(e, t, 2), $(e, r, 2)),
      a.effect === "buff13" && (i.atk++, (i.hp += 3), (i.maxHp += 3)),
      a.effect === "freeze" && (i.frozen = 1),
      a.effect === "sprouts")
    ) {
      let l = 0;
      for (let p of ye([0, 1, 2]))
        r.lanes[p].length < V(e) &&
          l < 2 &&
          (le(
            e,
            t,
            {
              name: "Broto Valente",
              fac: "wild",
              cost: 1,
              atk: 1,
              hp: 4,
              maxHp: 4,
              icon: "🌱",
              text: "Guarda.",
              kw: ["Guarda"],
              type: "unit",
            },
            p,
          ),
          l++);
    }
    if (
      (a.effect === "wildHeal" &&
        (T(r, 4 + s),
        y(r).forEach((l) => (l.hp = Math.min(l.maxHp, l.hp + 2)))),
      a.effect === "ice2" &&
        (w(e, i, 2 + s, a),
        i?.hp > 0 && Math.random() < 0.25 && (i.frozen = 1)),
      a.effect === "blizzard" &&
        y(o)
          .slice()
          .forEach((l) => {
            (w(e, l, 1 + s, a), l.hp > 0 && (l.frozen = 1));
          }),
      a.effect === "shield" && (i.shield = !0),
      a.effect === "zero" &&
        (e.teams.forEach((l) => y(l).forEach((p) => (p.frozen = 1))),
        C(e, t, 1)),
      a.effect === "mechBuff" && (i.atk++, i.hp++, i.maxHp++, (i.shield = !0)),
      a.effect === "overclock" &&
        ((i.atk += 3), (i.tempAtk = (i.tempAtk || 0) + 3), w(e, i, 1, a)),
      a.effect === "missile")
    ) {
      let l = qe(o);
      l && w(e, l, 4 + s, a);
    }
    if (
      (a.effect === "recycle" && C(e, t, 1 + (t.deathsTurn ? 1 : 0)),
      a.effect === "arcane2" && w(e, i, 2 + s, a),
      a.effect === "swap")
    ) {
      let l = i.atk;
      ((i.atk = Math.max(0, i.hp)),
        (i.hp = Math.max(1, l)),
        (i.maxHp = Math.max(i.maxHp, i.hp)));
    }
    if (a.effect === "duplicate" && n?.team === t.team) {
      let l = r.lanes[n.lane];
      if (l.length < V(e)) {
        let p = A(i, t.slot);
        ((p.atk = p.hp = p.maxHp = 1),
          (p.name += " Eco"),
          (p.age = 0),
          l.push(p));
      }
    }
    (a.effect === "forecast" &&
      (C(e, t, 2),
      t.hand.length &&
        t.deck.unshift(
          t.hand.splice((Math.random() * t.hand.length) | 0, 1)[0],
        )),
      a.effect === "coin" && (Math.random() < 0.5 ? (t.mana += 2) : C(e, t, 1)),
      a.effect === "bothDraw" && e.players.forEach((l) => C(e, l, 2)),
      a.effect === "purify" && (T(r, 3), (i.frozen = 0)),
      y(r)
        .filter((l) => l.ownerSlot === t.slot)
        .forEach((l) => {
          l.spellGrow && (l.atk++, (l.tempAtk = (l.tempAtk || 0) + 1));
        }),
      t.spellsTurn++,
      (t.nextDiscount = 0),
      t.spellsTurn === 2 &&
        y(r)
          .filter((l) => l.ownerSlot === t.slot)
          .forEach((l) => {
            l.manaGrow && (l.atk++, l.hp++, l.maxHp++);
          }));
  }
  function ae(e) {
    return e.type === "unit"
      ? e.atk + e.hp * 0.72 + (e.kw || []).length * 0.8
      : e.cost * 1.45 + 1.8;
  }
  function We(e, t) {
    let { own: a, neutral: n } = xe(e.hero),
      o = [...a, ...a, ...n],
      i = (s) =>
        t === "common"
          ? s.cost <= 3
          : t === "rare"
            ? s.cost >= 2 && s.cost <= 5
            : s.cost >= 4;
    return (o.some(i) && (o = o.filter(i)), o);
  }
  function Ee(e, t) {
    let s = globalThis.ArcanaEvolution?.draftOptions?.(e, t, L, (a) =>
      A(a, e.slot),
    );
    if (s?.length === 2) return s;
    let a = We(e, t),
      n = b(a),
      r = a
        .filter((i) => i.name !== n.name)
        .sort((i, s) => Math.abs(ae(i) - ae(n)) - Math.abs(ae(s) - ae(n))),
      o = b(r.slice(0, Math.min(6, r.length)));
    return [A(n, e.slot), A(o || n, e.slot)];
  }
  function Ze(e) {
    if (e <= 2) return "common";
    if (e % 8 === 0) return "legendary";
    if (e % 3 === 0) return "rare";
    let t = Math.random();
    return e >= 6 && t < 0.12 ? "legendary" : t < 0.38 ? "rare" : "common";
  }
  function Ke(e, t) {
    t.chest = { rarity: e.rarity, options: Ee(t, e.rarity) };
  }
  function Xe(e, t) {
    if (
      (t.turns++,
      t.relics.some((a) => a.effect === "library") &&
        t.turns % 4 === 0 &&
        C(e, t, 1),
      t.relics.some((a) => a.effect === "voidEye") &&
        Math.random() < 0.3 &&
        C(e, t, 1),
      t.relics.some((a) => a.effect === "hourglass") &&
        Math.random() < 0.25 &&
        t.manaBonus++,
      t.relics.some((a) => a.effect === "factory") && t.turns % 3 === 0)
    ) {
      let a = L.find((r) => r.name === "Drone Sucateiro"),
        n = [0, 1, 2].filter((r) => O(e, t).lanes[r].length < V(e));
      a && n.length && le(e, t, a, b(n));
    }
  }
  function ke(e) {
    if (e.over) return;
    let t = re(e);
    ((t.cardsTurn = t.spellsTurn = t.unitsTurn = t.deathsTurn = 0),
      (t.wildTriggers = 0),
      Xe(e, t));
    let a = F[e.mode];
    ((t.maxMana = Math.min(
      a.maxMana,
      Math.max(a.startMana, t.turns + (t.manaBonus || 0)),
    )),
      (t.mana = t.maxMana + t.bonusNext),
      (t.bonusNext = 0),
      Ke(e, t));
  }
  function et(e, t) {
    let a = O(e, t),
      n = ne(e, t);
    for (let r = 0; r < 3; r++)
      for (let o of [...a.lanes[r]]) {
        if (e.over || o.ownerSlot !== t.slot) continue;
        if (o.age < 1) {
          o.age++;
          continue;
        }
        if (o.frozen) {
          (o.frozen--,
            o.frozen ||
              (be(e, o)?.relics.some((Ge) => Ge.effect === "frostRelic") &&
                ((o.hp += 2), (o.maxHp += 2))));
          continue;
        }
        let i = n.lanes[r],
          s = i.find((p) => (p.kw || []).includes("Guarda")) || i[0],
          l = o.atk;
        if (
          (o.pack && a.lanes[r].length > 1 && l++,
          o.frostPredator && s?.frozen && (l += o.frostPredator),
          s)
        ) {
          let p = w(e, s, l, o);
          ((o.kw || []).includes("Vampirismo") && T(a, p),
            s.hp > 0 && w(e, o, s.atk, s));
        } else {
          let p = l + (o.heroBonus || 0);
          ($(e, n, p), (o.kw || []).includes("Vampirismo") && T(a, p));
        }
      }
  }
  function tt(e, t) {
    let a = O(e, t),
      n = ne(e, t);
    t.relics.some((r) => r.effect === "forestHeart") &&
      y(a).length >= 3 &&
      T(a, 1);
    for (let r of y(a).filter((o) => o.ownerSlot === t.slot))
      (r.endPing && $(e, n, r.endPing),
        r.tempAtk && ((r.atk -= r.tempAtk), (r.tempAtk = 0)));
  }
  function at(e) {
    let t = b(me);
    ((e.event = t),
      t.effect === "meteor" &&
        e.teams.forEach((a) =>
          y(a)
            .slice()
            .forEach((n) => w(e, n, 1)),
        ),
      t.effect === "mana" && e.players.forEach((a) => a.manaBonus++),
      t.effect === "blood" &&
        (e.teams.forEach((a) => $(e, a, 1)),
        e.teams.forEach((a) => y(a).forEach((n) => n.atk++))),
      t.effect === "fog" &&
        e.teams.forEach((a) => {
          let n = se(a);
          n && (n.shield = !0);
        }),
      t.effect === "spring" &&
        e.teams.forEach((a) =>
          y(a).forEach((n) => (n.hp = Math.min(n.maxHp, n.hp + 2))),
        ),
      t.effect === "knowledge" && e.players.forEach((a) => C(e, a, 1)));
  }
  function Ae(e) {
    if (e.mode !== "raidcoop") return;
    let t = e.teams[1],
      a = Math.max(0, t.hp) / t.maxHp;
    e.bossPhase = a <= 0.33 ? 3 : a <= 0.66 ? 2 : 1;
  }
  function Se(e, t = 1) {
    let a = e.teams[1],
      n = L.filter((r) => r.fac === "void" && r.type === "unit");
    for (let r = 0; r < t; r++) {
      let o = [0, 1, 2].filter((s) => a.lanes[s].length < V(e));
      if (!o.length) return;
      let i = A(b(n), -1);
      ((i.atk += e.bossPhase - 1),
        (i.hp += e.bossPhase),
        (i.maxHp = i.hp),
        a.lanes[b(o)].push(i));
    }
  }
  function nt(e) {
    let t = { slot: -1, team: 1, hero: { id: "architect" } },
      a = e.teams[1],
      n = e.teams[0];
    for (let r = 0; r < 3; r++)
      for (let o of [...a.lanes[r]]) {
        if (e.over) break;
        if (o.age < 1) {
          o.age++;
          continue;
        }
        if (o.frozen) {
          o.frozen--;
          continue;
        }
        let i = n.lanes[r],
          s = i.find((p) => (p.kw || []).includes("Guarda")) || i[0],
          l = o.atk + (e.bossPhase - 1);
        s ? (w(e, s, l, o), s.hp > 0 && w(e, o, s.atk, s)) : $(e, n, l);
      }
  }
  function rt(e) {
    if ((Ae(e), Se(e, e.bossPhase >= 3 ? 2 : 1), nt(e), e.over)) return;
    let t = e.teams[0];
    if ((e.bossPhase >= 2 && $(e, t, e.bossPhase), e.bossPhase === 3)) {
      let a = se(t);
      a && (a.frozen = 1);
    }
  }
  function ot(e, t) {
    let a = N.find((r) => r.id === e.heroId) || N[0],
      n = F[t],
      o = globalThis.ArcanaEvolution?.rules?.[e.classId]
        ? e.classId
        : "vanguard",
      i = {
        slot: e.slot,
        index: e.slot,
        clientId: e.clientId || "",
        name: e.name || `Jogador ${e.slot + 1}`,
        team: t === "duo" ? e.slot % 2 : 0,
        hero: a,
        classId: o,
        hand: [],
        deck: [],
        mana: n.startMana,
        maxMana: n.startMana,
        manaBonus: 0,
        bonusNext: 0,
        nextDiscount: 0,
        relics: [],
        fatigue: 0,
        turns: 0,
        cardsTurn: 0,
        spellsTurn: 0,
        unitsTurn: 0,
        deathsTurn: 0,
        wildTriggers: 0,
        chest: null,
        rerolls: n.rerolls,
        connected: !0,
      };
    return (
      (i.deck =
        globalThis.ArcanaEvolution?.buildDeck?.(
          i,
          globalThis.__ARCANA?.cards || L,
          (r) => A(r, e.slot),
          ye,
        ) || Ue(a)),
      i
    );
  }
  function Pe({ mode: e = "duo", members: t = [] } = {}) {
    let a = F[e];
    if (!a) throw new Error("Modo party inválido");
    if (t.length < a.minPlayers || t.length > a.maxPlayers)
      throw new Error("Quantidade de jogadores inválida");
    let n = t.map((s) => ot(s, e)).sort((s, l) => s.slot - l.slot),
      r = e === "duo" ? a.teamHp : a.teamHp + n.length * 8,
      o = e === "duo" ? a.teamHp : 160 + Math.max(0, n.length - 2) * 40,
      i = {
        mode: e,
        rev: 0,
        players: n,
        teams: [
          { id: 0, hp: r, maxHp: r, lanes: [[], [], []], frostRound: -1 },
          { id: 1, hp: o, maxHp: o, lanes: [[], [], []], frostRound: -1 },
        ],
        order: n.map((s) => s.slot),
        turnPos: 0,
        turnCount: 0,
        round: 1,
        rarity: "common",
        event: null,
        over: !1,
        winnerTeam: null,
        bossPhase: 1,
      };
    return (n.forEach(je), e === "raidcoop" && Se(i, 2), ke(i), i);
  }
  function re(e) {
    return e.players.find((t) => t.slot === e.order[e.turnPos]);
  }
  function oe(e) {
    return re(e)?.slot ?? null;
  }
  function it(e) {
    let t = re(e);
    if ((et(e, t), tt(e, t), !e.over)) {
      if (
        ((e.turnPos = (e.turnPos + 1) % e.order.length),
        e.turnCount++,
        e.turnPos === 0)
      ) {
        if ((e.mode === "raidcoop" && rt(e), e.over)) return;
        (e.round++,
          (e.rarity = Ze(e.round)),
          (e.event = null),
          e.round % 3 === 0 && at(e));
      }
      ke(e);
    }
  }
  function K(e, t, a) {
    if (!e || e.over || t !== oe(e) || !a) return !1;
    let n = re(e);
    if (n.chest && a.type !== "CHEST" && a.type !== "REROLL") return !1;
    if (a.type === "CHEST") {
      let r = Number(a.index);
      return !n.chest || ![0, 1].includes(r) || n.hand.length >= we(n)
        ? !1
        : (n.hand.push(n.chest.options[r]), (n.chest = null), !0);
    }
    if (a.type === "REROLL")
      return !n.chest || n.rerolls <= 0
        ? !1
        : (n.rerolls--, (n.chest.options = Ee(n, n.chest.rarity)), !0);
    if (a.type === "PLAY") {
      let r = n.hand.findIndex((s) => s.id === a.card);
      if (r < 0) return !1;
      let o = n.hand[r],
        i = ie(n, o);
      if (n.mana < i) return !1;
      if (o.type === "unit") {
        if (
          a.lane == null ||
          O(e, n).lanes[a.lane]?.length >= V(e) ||
          ((n.mana -= i), n.hand.splice(r, 1), !le(e, n, o, a.lane))
        )
          return !1;
      } else if (o.type === "relic")
        ((n.mana -= i),
          n.hand.splice(r, 1),
          n.relics.length >= 3 && n.relics.shift(),
          n.relics.push(o));
      else {
        if (!Qe(e, n, o, a.target)) return !1;
        ((n.mana -= i), n.hand.splice(r, 1), Ye(e, n, o, a.target));
      }
      return (
        n.cardsTurn++,
        n.hero.id === "gear" && n.cardsTurn % 3 === 0 && C(e, n, 1),
        ge(e),
        !0
      );
    }
    return a.type === "END" ? (it(e), !0) : !1;
  }
  function st(e) {
    return {
      id: e.id,
      ownerSlot: e.ownerSlot,
      name: e.name,
      fac: e.fac,
      icon: e.icon,
      text: e.text,
      kw: e.kw || [],
      atk: e.atk,
      hp: e.hp,
      maxHp: e.maxHp,
      frozen: e.frozen,
      shield: e.shield,
    };
  }
  function he(e) {
    return {
      slot: e.slot,
      team: e.team,
      name: e.name,
      hero: e.hero,
      classId: e.classId,
      handCount: e.hand.length,
      mana: e.mana,
      maxMana: e.maxMana,
      connected: e.connected,
    };
  }
  function de(e, t) {
    let a = e.players.find((n) => n.slot === t);
    if (!a) throw new Error("Slot inválido");
    return {
      rev: e.rev,
      mode: e.mode,
      slot: t,
      round: e.round,
      rarity: e.rarity,
      event: e.event,
      bossPhase: e.bossPhase,
      currentSlot: oe(e),
      over: e.over,
      winnerTeam: e.winnerTeam,
      teams: e.teams.map((n) => ({
        id: n.id,
        hp: n.hp,
        maxHp: n.maxHp,
        lanes: n.lanes.map((r) => r.map(st)),
      })),
      members: e.players.map(he),
      me: {
        ...he(a),
        rerolls: a.rerolls,
        deck: a.deck.length,
        relics: a.relics.map((n) => ({
          name: n.name,
          icon: n.icon,
          effect: n.effect,
        })),
        hand: a.hand.map((n) => ({ ...n, playCost: ie(a, n) })),
        chest: a.chest
          ? {
              rarity: a.chest.rarity,
              rerolls: a.rerolls,
              options: a.chest.options.map((n) => ({
                ...n,
                playCost: ie(a, n),
              })),
            }
          : null,
      },
    };
  }
  function Re(e) {
    return (e.rev++, e.rev);
  }
  function ce(e, t, a) {
    let n = e.players.find((r) => r.slot === t);
    n && (n.connected = !!a);
  }
  var Te = "arcana-party-client-v1",
    He =
      localStorage.getItem(Te) ||
      (() => {
        let e =
          "c-" + Math.random().toString(36).slice(2) + Date.now().toString(36);
        return (localStorage.setItem(Te, e), e);
      })(),
    v = null,
    x = null,
    E = "",
    D = null,
    P = 0,
    S = null,
    R = null,
    j = new Map(),
    h = null,
    m = null,
    d = null,
    I = !1,
    k = null,
    g = null,
    fe = null,
    X = 0,
    J = !1,
    f = (e) => v?.querySelector("#" + e);
  var lt = () => String(Math.floor(1e5 + Math.random() * 9e5)),
    z = () => F[x],
    dt = (e) => e === "duo" || e === "raidcoop";
  function ct() {
    if (v) return;
    ((v = document.createElement("section")),
      (v.id = "partyOverlay"),
      (v.className = "partyOverlay hidden"),
      (v.innerHTML = '<div id="partyRoot"></div>'),
      document.body.appendChild(v));
    let e = document.createElement("style");
    ((e.textContent = `
.partyOverlay{position:fixed;inset:0;z-index:10000;background:radial-gradient(circle at 50% 0,#192552 0,#080b18 48%,#050711 100%);color:#eef4ff;overflow:auto;padding:env(safe-area-inset-top) 10px env(safe-area-inset-bottom)}.partyOverlay.hidden{display:none}.partyWrap{max-width:920px;margin:0 auto;padding:16px 0 28px}.partyHead{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:12px}.partyBack,.partyBtn{border:1px solid #ffffff2c;background:#11182d;color:#fff;border-radius:13px;padding:11px 14px;font-weight:900}.partyBtn.primary{background:linear-gradient(135deg,#4be2ff,#776cff);color:#07101b;border:0}.partyBtn.danger{background:#401726}.partyTitle{text-align:center}.partyTitle b{display:block;font-size:22px}.partyTitle span{font-size:10px;letter-spacing:1.5px;opacity:.68}.partyPanel{background:#0c1122e8;border:1px solid #ffffff1d;border-radius:22px;padding:16px;box-shadow:0 22px 70px #0008}.partyActions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.partyCode{font-size:38px;font-weight:1000;letter-spacing:8px;text-align:center;margin:12px 0;color:#7be8ff}.partyInput{width:100%;box-sizing:border-box;border:1px solid #ffffff2a;border-radius:14px;background:#080d1c;color:#fff;font-size:28px;text-align:center;letter-spacing:8px;padding:13px}.partyRoster{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin:12px 0}.partyMember{border:1px solid #ffffff1c;border-radius:14px;padding:10px;background:#ffffff08}.partyMember.off{opacity:.45}.partyMember .row{display:flex;justify-content:space-between;gap:8px}.partyMember small{opacity:.65}.heroParty{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.heroParty button{min-height:82px;border:1px solid #ffffff22;border-radius:14px;color:#fff;background:#11182c;padding:7px}.heroParty button.sel{outline:2px solid #71e6ff}.heroParty .ico{font-size:27px}.partyStatus{text-align:center;font-size:11px;opacity:.75;margin:10px}.partyGameTop{display:grid;grid-template-columns:1fr auto 1fr;gap:8px;align-items:center}.nexus{border:1px solid #ffffff24;border-radius:14px;padding:8px;text-align:center}.nexus strong{display:block;font-size:22px}.partyRound{text-align:center;font-size:11px}.partyBoard{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin:10px 0}.pLane{min-height:245px;border:1px solid #ffffff1b;border-radius:15px;background:#090e1ddd;padding:6px;display:grid;grid-template-rows:1fr auto 1fr;gap:5px}.pLane.valid{outline:2px solid #ffd863}.pZone{display:grid;grid-template-columns:repeat(2,1fr);gap:4px;align-content:center}.pUnit{border:1px solid #ffffff1e;border-radius:10px;padding:5px;background:#16203a;font-size:8px;min-height:58px}.pUnit.valid{outline:2px solid #ffe56d}.pUnit .uico{font-size:21px}.pUnit .stats{display:flex;justify-content:space-between;font-weight:900}.pMid{text-align:center;font-size:8px;opacity:.45}.partyResources{display:flex;gap:7px;align-items:center;overflow:auto;font-size:10px;margin:7px 0}.partyHand{display:flex;gap:7px;overflow-x:auto;padding:4px 1px 12px}.pCard{flex:0 0 118px;min-height:162px;border:1px solid #ffffff20;border-radius:14px;padding:7px;background:#11182c;color:#fff;text-align:left;position:relative}.pCard.disabled{opacity:.42}.pCard.sel{outline:2px solid #ffe063}.pCost{position:absolute;top:6px;left:6px;border-radius:50%;background:#15234a;border:1px solid #6ee8ff;width:27px;height:27px;display:grid;place-items:center;font-weight:1000}.pCard .cico{text-align:center;font-size:31px;margin-top:10px}.pCard b{display:block;text-align:center;font-size:10px;margin:4px}.pCard p{font-size:8px;line-height:1.25;opacity:.78}.pRosterMini{display:flex;gap:5px;overflow:auto;margin:7px 0}.pChip{white-space:nowrap;border:1px solid #ffffff1b;border-radius:20px;padding:5px 8px;font-size:8px}.pChip.current{outline:1px solid #ffe36c}.partyChest{position:fixed;inset:0;z-index:10003;background:#050814e8;display:grid;place-items:center;padding:12px}.partyChest.hidden{display:none}.partyChestBox{width:min(680px,95vw);border:1px solid #ffffff25;border-radius:24px;background:#10172d;padding:16px;text-align:center}.partyChestCards{display:grid;grid-template-columns:1fr 1fr;gap:8px}.partyGameOver{position:fixed;inset:0;z-index:10004;background:#050814e8;display:grid;place-items:center}.partyGameOver>div{background:#10172d;border:1px solid #ffffff25;border-radius:24px;padding:25px;text-align:center;width:min(420px,88vw)}
@media(max-width:520px){.partyRoster{grid-template-columns:1fr}.heroParty{grid-template-columns:repeat(2,1fr)}.partyBoard{gap:4px}.pLane{min-height:220px;padding:4px}.pZone{grid-template-columns:1fr 1fr}.pCard{flex-basis:108px}}
`),
      document.head.appendChild(e));
  }
  function ft() {
    (ct(), v.classList.remove("hidden"));
  }
  function H() {
    ((J = !0), clearTimeout(fe));
    try {
      R?.close();
    } catch {}
    for (let e of j.values())
      try {
        e.close();
      } catch {}
    try {
      S?.destroy();
    } catch {}
    ((S = R = null),
      j.clear(),
      (h = m = d = null),
      (I = !1),
      (k = g = null),
      v?.classList.add("hidden"),
      document.getElementById("home")?.classList.remove("hidden"));
  }
  function _(e) {
    (ft(), (f("partyRoot").innerHTML = e));
  }
  function Q(e, t = "MULTIPLAYER CROSSPLAY") {
    return `<div class="partyHead"><button id="pBack" class="partyBack">‹</button><div class="partyTitle"><b>${e}</b><span>${t}</span></div><span style="width:42px"></span></div>`;
  }
  function ut(e) {
    ((x = e), (E = ""), (D = null), (h = m = d = null), (J = !1), pt());
  }
  function pt() {
    (_(
      `<div class="partyWrap">${Q(z().name)}<div class="partyPanel"><p style="text-align:center">${x === "duo" ? "Quatro jogadores, dois times, Nexus e rotas compartilhados." : "Forme um grupo de 2 a 4 jogadores contra o Arquiteto do Vazio."}</p><div class="partyActions"><button id="pCreate" class="partyBtn primary">CRIAR SALA</button><button id="pJoinOpen" class="partyBtn">ENTRAR</button></div><div id="pJoinBox" style="display:none;margin-top:12px"><input id="pCodeInput" class="partyInput" maxlength="6" inputmode="numeric" placeholder="000000"><button id="pJoin" class="partyBtn primary" style="width:100%;margin-top:8px">CONECTAR</button></div></div></div>`,
    ),
      (f("pBack").onclick = H),
      (f("pCreate").onclick = ht),
      (f("pJoinOpen").onclick = () => {
        ((f("pJoinBox").style.display = "block"), f("pCodeInput").focus());
      }),
      (f("pJoin").onclick = () =>
        pe(f("pCodeInput").value.replace(/\D/g, ""))));
  }
  function mt(e) {
    return x === "duo" ? e % 2 : 0;
  }
  function ht() {
    if (typeof Peer > "u") {
      alert("Multiplayer não carregou. Verifique a internet.");
      return;
    }
    ((J = !1),
      (D = "host"),
      (P = 0),
      (E = lt()),
      (h = {
        mode: x,
        room: E,
        members: [
          {
            slot: 0,
            clientId: He,
            name: "Jogador 1",
            heroId: null,
            classId: (() => {
              try {
                return (
                  JSON.parse(localStorage.getItem("arcana_profile_v2") || "{}")
                    .classId || "vanguard"
                );
              } catch {
                return "vanguard";
              }
            })(),
            ready: !1,
            connected: !0,
          },
        ],
      }),
      U(),
      (S = new Peer(`arcana-party-v1-${E}`, { secure: !0, debug: 0 })),
      S.on("open", () => U("Sala pronta.")),
      S.on("connection", yt),
      S.on("error", (e) => U("Erro de rede: " + e.type)));
  }
  function yt(e) {
    let t = e.metadata || {};
    (e.on("open", () => {
      if (t.version !== te || t.mode !== x) {
        (e.send({ t: "ERROR", message: "Versão ou modo incompatível." }),
          e.close());
        return;
      }
      let a = h.members.find((n) => n.clientId === t.clientId);
      if (a && a.connected) {
        (e.send({ t: "ERROR", message: "Este jogador já está conectado." }),
          e.close());
        return;
      }
      if (a) a.connected = !0;
      else {
        let n = new Set(h.members.map((o) => o.slot)),
          r = null;
        for (let o = 1; o < z().maxPlayers; o++)
          if (!n.has(o)) {
            r = o;
            break;
          }
        if (r == null) {
          (e.send({ t: "ERROR", message: "Sala cheia." }), e.close());
          return;
        }
        ((a = {
          slot: r,
          clientId: t.clientId || xt(),
          name: `Jogador ${r + 1}`,
          heroId: null,
          classId: globalThis.ArcanaEvolution?.rules?.[t.classId]
            ? t.classId
            : "vanguard",
          ready: !1,
          connected: !0,
        }),
          h.members.push(a));
      }
      ((e.__slot = a.slot),
        j.set(a.slot, e),
        m && ce(m, a.slot, !0),
        e.send({ t: "WELCOME", slot: a.slot, mode: x, room: E, version: te }),
        m ? (e.send({ t: "START" }), Z()) : Y(),
        U("Jogador conectado."));
    }),
      e.on("data", (a) => vt(e.__slot, a)),
      e.on("close", () => $e(e.__slot)),
      e.on("error", () => $e(e.__slot)));
  }
  function xt() {
    return "guest-" + Math.random().toString(36).slice(2);
  }
  function $e(e) {
    if (e == null) return;
    j.delete(e);
    let t = h?.members.find((a) => a.slot === e);
    (t && (t.connected = !1),
      m ? (ce(m, e, !1), Z(), setTimeout(() => bt(e), 3e4)) : Y(),
      U("Um jogador desconectou. Aguardando reconexão."));
  }
  function bt(e) {
    if (
      !m ||
      m.over ||
      oe(m) !== e ||
      h.members.find((n) => n.slot === e)?.connected
    )
      return;
    (m.players.find((n) => n.slot === e)?.chest &&
      K(m, e, { type: "CHEST", index: 0 }),
      K(m, e, { type: "END" }),
      Z());
  }
  function vt(e, t) {
    if (!t || typeof t != "object") return;
    if (m) {
      if (t.t === "ACT") {
        let n = K(m, e, t.a);
        (Z(), n || Ie(e, { t: "NOTICE", message: "Jogada inválida." }));
      }
      return;
    }
    let a = h.members.find((n) => n.slot === e);
    a &&
      (t.t === "HERO" &&
        N.some((n) => n.id === t.id) &&
        ((a.heroId = t.id), (a.ready = !1), Y()),
      t.t === "READY" && ((a.ready = !!t.value && !!a.heroId), Y()));
  }
  function Ie(e, t) {
    let a = j.get(e);
    a?.open && a.send(t);
  }
  function Y() {
    let e = {
      mode: x,
      room: E,
      members: h.members.map((t) => ({ ...t, team: mt(t.slot) })),
    };
    for (let t of j.values()) t.open && t.send({ t: "LOBBY", l: e });
    U();
  }
  function pe(e, t = !1) {
    if (e.length !== 6) {
      alert("Digite os 6 números da sala.");
      return;
    }
    if (typeof Peer > "u") {
      alert("Multiplayer não carregou.");
      return;
    }
    ((D = "guest"), (E = e), (J = !1), t || ze());
    try {
      S?.destroy();
    } catch {}
    ((S = new Peer(void 0, { secure: !0, debug: 0 })),
      S.on("open", () => {
        ((R = S.connect(`arcana-party-v1-${E}`, {
          reliable: !0,
          serialization: "json",
          metadata: {
            version: te,
            mode: x,
            clientId: He,
            classId: (() => {
              try {
                return (
                  JSON.parse(localStorage.getItem("arcana_profile_v2") || "{}")
                    .classId || "vanguard"
                );
              } catch {
                return "vanguard";
              }
            })(),
          },
        })),
          R.on("open", () => {
            X = 0;
          }),
          R.on("data", gt),
          R.on("close", Me),
          R.on("error", Me));
      }),
      S.on("error", (a) => {
        J || Be("Erro: " + a.type);
      }));
  }
  function ze() {
    (_(
      `<div class="partyWrap">${Q(z().name)}<div class="partyPanel"><h2 style="text-align:center">Conectando à sala ${E}</h2><p class="partyStatus">Procurando o anfitrião...</p></div></div>`,
    ),
      (f("pBack").onclick = H));
  }
  function gt(e) {
    if (!(!e || typeof e != "object")) {
      if (e.t === "ERROR") {
        (alert(e.message), H());
        return;
      }
      if (e.t === "WELCOME") {
        ((P = e.slot), (x = e.mode), (E = e.room));
        return;
      }
      if (e.t === "LOBBY") {
        ((h = e.l), U());
        return;
      }
      if (e.t === "START") {
        W();
        return;
      }
      if (e.t === "STATE") {
        ((d = e.v), (I = !1), (k = g = null), W());
        return;
      }
      e.t === "NOTICE" && ((I = !1), alert(e.message));
    }
  }
  function Me() {
    J || Be("Conexão perdida. Reconectando...");
  }
  function Be(e) {
    if ((clearTimeout(fe), X++, X > 8)) {
      wt();
      return;
    }
    (v && f("pNetStatus") && (f("pNetStatus").textContent = e),
      (fe = setTimeout(() => pe(E, !0), Math.min(6e3, 1200 * X))));
  }
  function wt() {
    (_(
      `<div class="partyWrap">${Q("Conexão perdida")}<div class="partyPanel"><p>Não foi possível reconectar automaticamente.</p><button id="pRetry" class="partyBtn primary" style="width:100%">TENTAR NOVAMENTE</button></div></div>`,
    ),
      (f("pBack").onclick = H),
      (f("pRetry").onclick = () => {
        ((X = 0), pe(E, !0));
      }));
  }
  function Ct(e) {
    if (D === "host") {
      let t = h.members.find((a) => a.slot === 0);
      ((t.heroId = e), (t.ready = !1), Y());
    } else R?.send({ t: "HERO", id: e });
  }
  function Et() {
    let e = h.members.find((t) => t.slot === P);
    e?.heroId &&
      (D === "host"
        ? ((e.ready = !e.ready), Y())
        : R?.send({ t: "READY", value: !e.ready }));
  }
  function ue() {
    if (D !== "host" || !h) return !1;
    let e = h.members.filter((t) => t.connected);
    return e.length < z().minPlayers || (x === "duo" && e.length !== 4)
      ? !1
      : e.every((t) => t.heroId && t.ready);
  }
  function kt() {
    if (!ue()) return;
    let e = h.members
      .filter((t) => t.connected)
      .map((t) => ({
        slot: t.slot,
        clientId: t.clientId,
        name: t.name,
        heroId: t.heroId,
        classId: t.classId,
      }));
    m = Pe({ mode: x, members: e });
    for (let t of j.values()) t.open && t.send({ t: "START" });
    Z();
  }
  function U(e = "") {
    if (!h) {
      ze();
      return;
    }
    let t = [...h.members].sort((n, r) => n.slot - r.slot),
      a = t.find((n) => n.slot === P);
    (_(
      `<div class="partyWrap">${Q(z().name, x === "duo" ? "2V2 · QUATRO JOGADORES" : "RAID · 2–4 JOGADORES")}<div class="partyPanel"><div class="partyCode">${E}</div><p id="pNetStatus" class="partyStatus">${e || "Escolha seu campeão e marque PRONTO."}</p><div class="partyRoster">${t.map((n) => `<div class="partyMember ${n.connected ? "" : "off"}"><div class="row"><b>${n.team === 0 ? "🔷" : "🔶"} ${n.name}</b><span>${n.ready ? "✅" : "⌛"}</span></div><small>${n.heroId ? N.find((r) => r.id === n.heroId)?.name || "Herói" : "Sem herói"} · ${globalThis.ArcanaEvolution?.rules?.[n.classId]?.name || "Classe Arcana"} · ${n.connected ? "online" : "reconectando"}</small></div>`).join("")}</div><h3>Seu campeão</h3><div class="heroParty">${N.map((n) => `<button data-hero="${n.id}" class="${a?.heroId === n.id ? "sel" : ""}"><div class="ico">${n.icon}</div><b>${n.name}</b><small>${M[n.fac].name}</small></button>`).join("")}</div><div class="partyActions" style="margin-top:12px"><button id="pReady" class="partyBtn ${a?.ready ? "" : "primary"}">${a?.ready ? "CANCELAR PRONTO" : "PRONTO"}</button>${D === "host" ? `<button id="pStart" class="partyBtn primary" ${ue() ? "" : "disabled"}>INICIAR</button>` : '<button id="pShare" class="partyBtn">COPIAR CÓDIGO</button>'}</div></div></div>`,
    ),
      (f("pBack").onclick = H),
      v
        .querySelectorAll("[data-hero]")
        .forEach((n) => (n.onclick = () => Ct(n.dataset.hero))),
      (f("pReady").onclick = Et),
      f("pStart") &&
        ((f("pStart").disabled = !ue()), (f("pStart").onclick = kt)),
      f("pShare") &&
        (f("pShare").onclick = () => navigator.clipboard?.writeText(E)));
  }
  function Z() {
    if (m) {
      (Re(m), (d = de(m, 0)));
      for (let e of h.members)
        e.slot === 0 ||
          !e.connected ||
          Ie(e.slot, { t: "STATE", v: de(m, e.slot) });
      W();
    }
  }
  function q(e) {
    !d ||
      d.over ||
      d.currentSlot !== P ||
      I ||
      (D === "host"
        ? (K(m, P, e), Z())
        : ((I = !0), R?.send({ t: "ACT", a: e })));
  }
  function B() {
    return d?.me.team ?? 0;
  }
  function De() {
    return B() === 0 ? 1 : 0;
  }
  function At(e) {
    return d?.me.hand.find((t) => t.id === e);
  }
  function St(e) {
    if (d.currentSlot !== P || I || d.me.chest) return;
    let t = At(e);
    if (!(!t || t.playCost > d.me.mana)) {
      if (((k = t), t.type === "unit")) {
        ((g = "lane"), W());
        return;
      }
      if (t.type === "relic" || t.target === "none") {
        ((k = g = null), q({ type: "PLAY", card: t.id }));
        return;
      }
      ((g = t.target || "any"), W());
    }
  }
  function Pt(e) {
    !k || g !== "lane" || q({ type: "PLAY", card: k.id, lane: e });
  }
  function Rt(e, t, a) {
    !k ||
      !["ally", "enemy", "any"].includes(g) ||
      (g === "ally" && e !== B()) ||
      (g === "enemy" && e === B()) ||
      q({ type: "PLAY", card: k.id, target: { team: e, lane: t, index: a } });
  }
  function Oe(e, t, a, n) {
    let r = M[e.fac] || M.neutral;
    return `<button class="pUnit ${k && ((g === "ally" && t === B()) || (g === "enemy" && t !== B()) || g === "any") ? "valid" : ""}" data-u="1" data-team="${t}" data-l="${a}" data-i="${n}" style="background:linear-gradient(180deg,${r.a}32,${r.b}22,#101426)"><div class="uico">${e.icon}</div><b>${e.name}</b><small>J${e.ownerSlot >= 0 ? e.ownerSlot + 1 : "BOSS"}</small><div class="stats"><span>⚔${e.atk}</span><span>${e.frozen ? "❄" : ""}${e.shield ? "🛡" : ""}♥${e.hp}</span></div></button>`;
  }
  function Tt(e) {
    let t = d.teams[B()],
      a = d.teams[De()];
    return `<div class="pLane ${k && g === "lane" && t.lanes[e].length < z().laneCap ? "valid" : ""}" data-lane="${e}"><div class="pZone">${a.lanes[e].map((r, o) => Oe(r, a.id, e, o)).join("") || "<span></span>"}</div><div class="pMid">ROTA ${e + 1}</div><div class="pZone">${t.lanes[e].map((r, o) => Oe(r, t.id, e, o)).join("") || "<span></span>"}</div></div>`;
  }
  function $t(e) {
    let t = d.currentSlot !== P || I || d.me.chest || d.me.mana < e.playCost,
      a = k?.id === e.id,
      n = M[e.fac] || M.neutral;
    return `<button class="pCard ${t ? "disabled" : ""} ${a ? "sel" : ""}" data-card="${e.id}" style="background:linear-gradient(180deg,${n.a}34,${n.b}22,#101426)"><span class="pCost">${e.playCost}</span><div class="cico">${e.icon}</div><b>${e.name}</b><p>${e.text}</p>${e.type === "unit" ? `<div class="stats"><span>⚔${e.atk}</span><span>♥${e.hp}</span></div>` : ""}</button>`;
  }
  function Mt(e, t) {
    let a = M[e.fac] || M.neutral;
    return `<button class="pCard" data-chest="${t}" style="width:100%;background:linear-gradient(180deg,${a.a}34,${a.b}22,#101426)"><span class="pCost">${e.playCost}</span><div class="cico">${e.icon}</div><b>${e.name}</b><p>${e.text}</p>${e.type === "unit" ? `<div class="stats"><span>⚔${e.atk}</span><span>♥${e.hp}</span></div>` : ""}</button>`;
  }
  function Ot() {
    let e = d.me.chest;
    if (!e || d.currentSlot !== P) return "";
    let t = ee[e.rarity] || ee.common;
    return `<div class="partyChest"><div class="partyChestBox"><div style="font-size:50px">${t.icon}</div><h2>${t.name}</h2><p>Escolha uma das duas recompensas.</p><div class="partyChestCards">${e.options.map(Mt).join("")}</div><button id="pReroll" class="partyBtn" style="margin-top:10px" ${d.me.rerolls <= 0 ? "disabled" : ""}>↻ REEMBARALHAR (${d.me.rerolls})</button></div></div>`;
  }
  function W() {
    if (!d) {
      (_(
        `<div class="partyWrap">${Q(z().name)}<div class="partyPanel"><p class="partyStatus">Sincronizando partida...</p></div></div>`,
      ),
        (f("pBack").onclick = H));
      return;
    }
    let e = d.teams[B()],
      t = d.teams[De()],
      a = d.members.find((r) => r.slot === d.currentSlot),
      n = d.over && d.winnerTeam === B();
    (_(
      `<div class="partyWrap">${Q(z().name, x === "duo" ? "NEXUS COMPARTILHADO · 2V2" : "GRUPO COOPERATIVO")}<div class="partyGameTop"><div class="nexus"><small>${x === "duo" ? "SEU TIME" : "GRUPO"}</small><strong>♥ ${Math.max(0, e.hp)}</strong></div><div class="partyRound">RODADA ${d.round}<br><b>${d.currentSlot === P ? "SUA VEZ" : a?.name || "—"}</b>${x === "raidcoop" ? `<br>CHEFE FASE ${d.bossPhase}` : ""}</div><div class="nexus"><small>${x === "duo" ? "RIVAIS" : "ARQUITETO"}</small><strong>♥ ${Math.max(0, t.hp)}</strong></div></div><div class="pRosterMini">${d.members.map((r) => `<span class="pChip ${r.slot === d.currentSlot ? "current" : ""}">${r.team === 0 ? "🔷" : "🔶"} J${r.slot + 1} ${r.hero.icon} · ${r.handCount}🃏 ${r.connected ? "" : "⚠"}</span>`).join("")}</div><div class="partyStatus">${d.event ? `${d.event.name} — ${d.event.text}` : `${ee[d.rarity]?.name || "Baú Comum"} · ${k ? (g === "lane" ? "escolha uma rota" : "escolha um alvo") : ""}`}</div><div class="partyBoard">${[0, 1, 2].map(Tt).join("")}</div><div class="partyResources"><b>💎 ${d.me.mana}/${d.me.maxMana}</b><span>📚 ${d.me.deck}</span><span>${globalThis.ArcanaEvolution?.rules?.[d.me.classId]?.icon || d.me.hero.icon} ${globalThis.ArcanaEvolution?.rules?.[d.me.classId]?.name || d.me.hero.name}</span><button id="pCancel" class="partyBtn" style="display:${k ? "block" : "none"}">CANCELAR</button><button id="pEnd" class="partyBtn primary" ${d.currentSlot !== P || I || d.me.chest ? "disabled" : ""}>ENCERRAR</button></div><div class="partyHand">${d.me.hand.map($t).join("")}</div>${Ot()}${d.over ? `<div class="partyGameOver"><div><div style="font-size:56px">${n ? "🏆" : "💀"}</div><h1>${n ? "VITÓRIA" : "DERROTA"}</h1><p>${x === "duo" ? "O Nexus adversário caiu." : "A Raid terminou."}</p><button id="pHome" class="partyBtn primary">VOLTAR AOS MODOS</button></div></div>` : ""}</div>`,
    ),
      (f("pBack").onclick = H),
      v
        .querySelectorAll("[data-card]")
        .forEach((r) => (r.onclick = () => St(r.dataset.card))),
      v.querySelectorAll("[data-lane]").forEach(
        (r) =>
          (r.onclick = (o) => {
            o.target.closest("[data-u]") || Pt(+r.dataset.lane);
          }),
      ),
      v.querySelectorAll("[data-u]").forEach(
        (r) =>
          (r.onclick = (o) => {
            (o.stopPropagation(),
              Rt(+r.dataset.team, +r.dataset.l, +r.dataset.i));
          }),
      ),
      v
        .querySelectorAll("[data-chest]")
        .forEach(
          (r) =>
            (r.onclick = () => q({ type: "CHEST", index: +r.dataset.chest })),
        ),
      f("pReroll") && (f("pReroll").onclick = () => q({ type: "REROLL" })),
      (f("pEnd").onclick = () => q({ type: "END" })),
      f("pCancel") &&
        (f("pCancel").onclick = () => {
          ((k = g = null), W());
        }),
      f("pHome") && (f("pHome").onclick = H));
  }
  globalThis.ArcanaPartyLive = {
    state: () => d,
    room: () => E,
    mode: () => x,
    role: () => D,
    close: H,
  };
  document.addEventListener(
    "click",
    (e) => {
      let t = e.target.closest?.(".modeTile[data-mode]");
      !t ||
        !dt(t.dataset.mode) ||
        (e.preventDefault(),
        e.stopPropagation(),
        e.stopImmediatePropagation(),
        ut(t.dataset.mode));
    },
    !0,
  );
})();
